**Icon** — Lucide glyph wrapper. Load the lucide CDN script once per page.

```jsx
<Icon name="upload" size={22} />
<Icon name="arrow-right" color="var(--rk-tomato-500)" />
```

name = any Lucide id. Requires <script src="https://unpkg.com/lucide@latest"></script> then icons hydrate automatically.
