**SectionHeading** — eyebrow + display title + intro; the standard section opener.

```jsx
<SectionHeading eyebrow="What we do" title="From PDF to polished site" intro="No reformatting headaches. Just a better reader experience." align="center" />
```

`inverse` for rhino/dark sections. sizes sm/md/lg control the title.
