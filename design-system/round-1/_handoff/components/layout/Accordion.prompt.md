**Accordion** — layered content that makes long toolkits scannable.

```jsx
<Accordion items={[{q:"How does it work?",a:"Upload your PDF; we handle the rest."},{q:"Is it accessible?",a:"Every site meets WCAG AA."}]} />
```

Single-open by default; set `multi` for many-open. `defaultOpen={[0]}`.
