**Input** — labelled text field for contact & signup forms.

```jsx
<Input label="Work email" type="email" icon="mail" placeholder="you@org.org" />
```

Optional `icon` (Lucide), `helper`, `error`. Focus shows the muffin-blue ring.
