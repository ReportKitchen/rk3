doc: 02--ecp-homeforgood

# GOLD(col) — columns/reading-order gold set (plans/columns-reading-order.md
# phase 0). This doc is the column stress-test: sidebar boxes interleave with
# two-column body text. Encoded from reviewer structure notes.
checks:
  # p4: the text "(“post-filing”) and after the court has determined…" was
  # emitted as a stray element; it is the CONTINUATION of the paragraph
  # "The goal of this paper is to highlight…" (note n4d471893bf: "column
  # problem — this is not a callout").
  - note: "GOLD(col) p4 (target): 'post-filing' continuation rejoins its paragraph"
    merge: ["The goal of this paper is to highlight", "post-filing”) and after the court has determined"]

  # p6: the "Defining Legal Terms" box definitions interleaved with the body
  # column (y-sort inside a two-column whole-page region). Source page order
  # (eyeballed, note n6cc849e5e5): LEFT column = "About this Paper" box, then
  # "Eviction's Impact" heading + body; RIGHT column = the legal-terms
  # reference box, read last. Fixed by the aside-interior column model
  # (phase 2); pinned here.
  - note: "GOLD(col) p6 (guard): 'About this Paper' box precedes 'Eviction's Impact'"
    order: ["About this Paper", "Eviction’s Impact"]

  - note: "GOLD(col) p6 (guard): definitions read AFTER the body column (no interleave)"
    order: ["The pressures of poverty, combined", "Just Cause – The requirement"]

  - note: "GOLD(col) p6 (guard): body text follows its 'Eviction's Impact' header"
    order: ["Eviction’s Impact", "More than a quarter of renters"]

  # p10: the magic-wand example (features/magic-wand.md) — "eviction can
  # proceed" is a stray fragment that belongs to the "Post-Eviction: after
  # the court has determined that an" list item (note nb37cb63343).
  - note: "GOLD(col) p10 (target): 'eviction can proceed' rejoins its list item"
    list: ["Upstream: to avoid housing instability", "Post-Eviction: after the court has determined that an eviction can proceed"]

  # GOLD(lists L6) p4 (guard): the eviction-cycle items keep their bold
  # leads ("Upstream:" etc. — owner note "missing initial bolds"), verified
  # fixed by the container work; frozen on the FIRST item only because the
  # later items carry a separate open defect (missing spaces at bold
  # boundaries: "AfterArrears/Pre-Filing" — source shows "After Arrears/
  # Pre-Filing"). NOTE: "increase housing instability" is the SOURCE's own
  # wording (verified on the page) — fidelity-first keeps it.
  - note: "GOLD(lists) p4 (guard): 'Upstream:' item renders with its bold lead"
    stage: analyze
    freeze:
      anchor: "Prevention strategies that increase housing"
      html: "<strong>Upstream:</strong> Prevention strategies that increase housing instability and reduce or eliminate the prospect of eviction."
