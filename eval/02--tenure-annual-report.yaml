doc: 02--tenure-annual-report
checks:
  # p13 (owner-minted 2026-07-02, CORRECTED against the source page image):
  # the original pair asserted 'It is a golden moment' BEFORE 'The Babassu
  # women approached' (and was minted twice) — the source page shows the
  # right column reading Babassu-women (top) THEN golden-moment (below it).
  # The defect the owner was flagging was real but different: both bottom
  # paragraphs were trapped in a phantom aside minted from the decorative
  # leaf watermark; dissolved via regionOverride kind=text (config,
  # 2026-07-02). These pins encode the source order.
  - note: "p13 (guard): 'They persuaded' reads in flow, before the right column"
    stage: analyze
    order:
    - They persuaded 17 municipalities
    - The Babassu women approached the Tenure Facil

  - note: "p13 (guard): right column reads Babassu-women then golden-moment"
    stage: analyze
    order:
    - The Babassu women approached the Tenure Facil
    - It is a golden moment. The new Brazilian Mini

  - merge:
    - The new scheme came with its own programme ma
    - leaders. The State Government entrusted our p

  # GOLD p31 (target, owner note 4005009f): the SOURCE line-breaks
  # "SUMMA-/RISE" inside an all-caps Q&A heading; dehyphenation misses the
  # caps case, leaving "SUMMA- RISE". Red at plant (v163).
  - note: "GOLD p31 (target): 'SUMMARISE' dehyphenates inside the all-caps question"
    stage: analyze
    freeze:
      anchor: "THE IMPACT OF FECOFUN ON THE FORESTS"
      html: "Q: HOW WOULD YOU SUMMARISE THE IMPACT OF FECOFUN ON THE FORESTS OF NEPAL?"

  # GOLD p54 (guards, owner note 9480e0a1): two side-by-side photo captions
  # ("hidden table" — person left, person right) were spliced when noted;
  # healed by the v157-163 ordering/caption work. Pinned: each caption is
  # one whole node, never fused with its neighbour. ("Protectd" is the
  # source's own typo — fidelity keeps it.)
  - note: "GOLD p54 (guard): the two photo captions stay separate nodes"
    stage: analyze
    split: ["Wider Guaramag, President of the", "Nixon Andy, Coordinator of the"]

  - note: "GOLD p54 (guard): left caption is whole"
    stage: analyze
    merge: ["Wider Guaramag, President of the", "Cofan Protectd Community of Sinagoe"]

  - note: "GOLD p54 (guard): right caption is whole"
    stage: analyze
    merge: ["Nixon Andy, Coordinator of the", "Protected Community of Sinagoe, Ecuador."]

  # GOLD(figures F3) p14 (target, note 46f46d38): the pale tree watermark
  # minted an aside that trapped two body paragraphs and scrambled the
  # section rundown (source: plain two-column flow, sections 2-7 in order).
  - note: "GOLD(fig) p14 (target): Section-7 + appendices paragraphs read in flow"
    in_flow: ["In the final Section 7, we look", "And for those who might still want more insights"]

  - note: "GOLD(fig) p14 (target): sections read in order (4 before 5)"
    order: ["In Section 4, What We Achieved", "In Section 5, we explain the How"]

  - note: "GOLD(fig) p14 (target): sections read in order (6 before 7)"
    order: ["In Section 6, we describe the With What", "In the final Section 7, we look"]

  # webified §3.4 floatPin: the p7 Nonette Royo portrait floats left (source
  # image; bottom-left of the page). Pinned via config so the lever's override
  # path stays wired and the correct float side is asserted.
  - note: "GOLD(fig) p7 (target, §3.4 floatPin): the portrait floats left"
    float: { nid: "neb75eac908", is: "left" }

  # GOLD(§6.2 tables) p8 acronyms glossary (owner note edd55787 "landed in the
  # wrong column"): single-line rows (term + short value on ONE line, uniform
  # COLOR but the term in a BOLD font) failed color-segmentation and dumped the
  # whole line into the term column, leaving the value column empty. Font-run
  # segmentation splits term|value. RED-AT-PLANT 2026-07-07: value merged into
  # cell 0, cell 1 empty (starts-with "National Land Agency" fails).
  - note: "GOLD(§6.2): p8 'ANT | National Land Agency' splits into two cells"
    cells: ["ANT", "National Land Agency"]
