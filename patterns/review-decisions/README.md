# Review Decisions

Reviewer decisions are recorded here as JSON or JSONL files using
`patterns/schemas/review-decision.schema.json`.

The first file-based decisions should use the same `document_id` and
`pattern_id` emitted by `patterns/out/<slug>.json`.

