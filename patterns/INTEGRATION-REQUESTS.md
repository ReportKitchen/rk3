# Pattern Track Integration Requests

No open requests.

The current implementation works around integration needs by reading
`output/pdfium/<slug>/ir.json` and writing regenerable snapshots to
`patterns/out/`.

