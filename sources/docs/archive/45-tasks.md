
# Whisk
- remove "almost there" and don't include "Reading your report" in the loop. those two give it away.
- the text is centered, but then it's adding periods, so the text shifts to the left.  Find a spot to center it but then don't let the added periods shift the text.

# trimmed
Atlantic council, "introducion" block -- switching from full to trimmed does nothing.  If its not long enough to trim, hide the option.

# icons
there's still a lot of icon duplication - and in the wild I have NEVER seen an icon set display where they repeat icons.  are you searching lucide names?  or just using ones you know?  because "money" returns a ton of options.  you can just pick at random and not re-use.  if you need to we can download the whole collection, or just a list of names.


# Accent / page length
- put page length first/left
- Accent, just have one custom color picker box.  nobody wants RK palette colors as their accent.  Start with a dark blue as default.


# Cover
They all look bad with the cover floated next to the title. Every title extends past the cover creating trapped white space.

Try these layouts:

- Put title first, then  
  - Cover floats right, in the first summary, alone
  - Cover floats right, in the first summary, in a shaded box, with embedded download button
  - Cover (small) in a full-width shaded band, after the first summary, along with repeat of the report title, date if we have it, authors if we have it.  download button under the cover.


# block drag-reorder

# persistence
- FYI I want the same graph paper/dropshadow on the full document as we have in the middle panel.
- Add a "clear formatting" or "normal text" option to the toolbar
- UGH the toolbar "link" throws the browser javascript dialog. I NEVER EVER want to see that dialog used.  It completely breaks the UI.  Link should pop up next to the toolbar, not in a modal.  and offer checkbox for open in new window.

# "content registry enforcement" from DEFERRED.  