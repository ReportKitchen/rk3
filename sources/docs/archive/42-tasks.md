-Exec summary has a natural title, but AI summary still says "AI summary"
do we have it write one?  or select one of "summary, intro, about" etc whichever they DONT have?


-change:

Introduction
How the page opens — your report’s own foreword and summary.

How the page opens — {n} sections pulled from your document {if ai }, plus a concise summary we drafted.

- it's a bit hard to tell at a glance which blocks are enabled and which aren't.  maybe just make the checkmark more prominent?